import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'protectosystem.com',
  appName: 'protectosystem.com',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
